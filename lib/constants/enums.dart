enum ConnectionStatus { mobile, none }

enum LocationStatus { whileInUse, denied, always }

enum BluetoothCheckState { on, off }

enum ServerStatus { connected, disconnected }

enum DataCheck { active, notactive }
